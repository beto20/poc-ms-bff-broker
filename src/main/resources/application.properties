spring.application.name=bff-broker

# Public-facing BFF port.
server.port=8080

# Downstream business-broker base URL. Override per environment.
business-broker.base-url=${BUSINESS_BROKER_URL:http://localhost:8081}
